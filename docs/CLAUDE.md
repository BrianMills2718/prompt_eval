# docs

This subtree contains repo-local documentation for `prompt_eval`.

## Route By Question

- architecture decisions -> `adr/`
- active roadmap and plan status -> `plans/`

## Working Rules

- Keep this parent at routing level.
- Scope and product-boundary decisions belong here or in root docs, not in
  package code comments.
