# hooks

This directory contains the installed git hook entrypoints for `prompt_eval`.

## Working Rules

- Prefer changing canonical templates or generators before editing installed
  hook files directly.
- Keep repo-local hook edits narrow and justified.
