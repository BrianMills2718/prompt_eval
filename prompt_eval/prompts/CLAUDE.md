# prompt_eval/prompts

This directory contains prompt templates owned by `prompt_eval`.

## Working Rules

- Keep prompts as data and align them with the code that renders them.
- Use this subtree for prompt-eval-specific templates, not shared global prompt
  assets that belong in `llm_client`.
